algoritmo "CONTAGEMREGRESSIVA"
var
   CONTADOR: INTEIRO
inicio
   CONTADOR <- 10
   ENQUANTO ((CONTADOR <= 10) E (CONTADOR >= 0)) FACA
            ESCREVAL (CONTADOR)
            CONTADOR <- CONTADOR - 1
   FIMENQUANTO
   ESCREVA ("CONTAGEM REGRESSIVA FINALIZADA")
fimalgoritmo